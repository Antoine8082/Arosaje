{% extends 'base.html.twig' %}
{% block body %}
    <h3>Liste demandes</h3>
    {% for post in posts %}
        <div class="card">
            <div class="title">{{ post.title }}</div>
        </div>
    {% endfor %}
{% endblock %}
